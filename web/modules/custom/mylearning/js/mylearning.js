(function ($, Drupal, drupalSettings) {
  Drupal.behaviors.mylearning = {
    attach: function (context, settings) {       
      
      console.log("My learning JS file is working!");
      
    }
  };

}(jQuery, Drupal, drupalSettings));
